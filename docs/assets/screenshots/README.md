# Screenshot Assets

Public README screenshots live here.

- `popover.png` - menu bar popover with power flow visible
- `dashboard.png` - dashboard overview with cards and power flow
- `settings.png` - settings window showing app behavior or telemetry options
- `menu-bar-styles.png` - generated menu bar style samples using PowerLens SVG assets

Use real app screenshots rather than generated mockups so the README accurately
matches the current UI. Crop only the surrounding desktop or menu-bar chrome
when it distracts from the app.
